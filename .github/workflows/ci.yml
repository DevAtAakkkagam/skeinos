name: CI

on:
  push:
    branches: [main]
  pull_request:

defaults:
  run:
    working-directory: extension

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: extension/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests (happy-dom)
        run: npm test

      - name: Browser tests (real Chromium)
        # GitHub's ubuntu runners ship Google Chrome; Playwright drives it via channel.
        run: npm run test:browser

      - name: Build extension
        run: npm run build

      - name: Package zip
        run: npm run zip

      - name: Upload extension zip
        uses: actions/upload-artifact@v4
        with:
          name: skeinos-chrome
          path: extension/.output/*.zip
          if-no-files-found: error
          # `.output` is a dot-directory; upload-artifact@v4 skips hidden paths
          # by default, so opt in or the zip is silently excluded.
          include-hidden-files: true
