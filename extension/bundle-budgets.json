{
  "$comment": "Bundle-size gate. Budgets are gzipped KB per logical entrypoint — see scripts/check-bundle-size.mjs. The content script is injected into every LLM tab, so it gets the tightest scrutiny. Bump a budget only with a deliberate reason in the commit.",
  "outDir": ".output/chrome-mv3",
  "entries": [
    {
      "name": "content-script",
      "$reason": "Bumped 65->90 for the input-action-bar change: the bar is a content-script shadow-DOM overlay (design D-2), so the content bundle now legitimately carries Preact + the floating/dialog primitives (useFloating, Zag dialog) it mounts. WXT inlines content-script deps, so lazy-loading would not reliably trim this entry.",
      "files": [
        "content-scripts/skeinos.js"
      ],
      "budgetKb": 90
    },
    {
      "name": "background",
      "files": [
        "background.js"
      ],
      "budgetKb": 20
    },
    {
      "name": "sidepanel",
      "$reason": "Dropped the bundled IBM Plex Mono face (labels now use the platform monospace stack), reclaiming ~14.6KB gzip across every entry. Bumped 110->118 for the remaining gap: the side panel carries the full app surface that landed after the original budget (onboarding stepper, instruction profiles, the docked input bar + prompt library), and the single bundled face (Urbanist) is shared design infra. Trimmed first, then bumped.",
      "html": "sidepanel.html",
      "budgetKb": 118
    },
    {
      "name": "options",
      "html": "options.html",
      "budgetKb": 60
    }
  ]
}